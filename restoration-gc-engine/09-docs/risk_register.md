# Risk Register

| # | Risk | Likelihood | Impact | Mitigation | Owner |
|---|---|---|---|---|---|
| 1 | State law changes (TX/FL/OK/IL) invalidate a seeded compliance rule or deadline | Medium | High | Quarterly lexicon review against primary sources; W7 weekly report surfaces `compliance_flag` spikes as an early signal | MJ + counsel |
| 2 | IL DOI Bulletin 2026-02 exposure — inadvertent paid PA lead-gen in Illinois | Low (if rule enforced) | Severe (Class 4 felony exposure, void PA contracts) | `il_no_pa_referral` block-severity rule; no paid-referral workflow exists in this build; explicit "do not launch in IL" gate in README recommendations | MJ |
| 3 | TCPA/FCC violation — AI voice or SMS dispatched without valid consent | Low (if enforced) | High (statutory damages, per-call/text) | Dual enforcement: W4 IF-node + `chk_consent_disclosure` DB constraint; W4 stays disabled until Retell consent-flow sign-off | MJ + counsel |
| 4 | CAN-SPAM violation — missing unsubscribe, physical address, or deceptive subject line | Low | High ($53,088/violation, 2025 adjustment) | HubSpot nurture template includes physical address + one-click unsubscribe by default; W7 monitors consent-rate drops | MJ |
| 5 | LinkedIn Community Management API access denied, revoked, or downgraded | Medium | Medium (loses automated posting/analytics, not outreach itself) | Human-in-the-loop design means core outreach doesn't depend on API access; app name avoids "Linked"/"In"; webhook re-validation monitored | MJ |
| 6 | LinkedIn UA §8.2 violation — automated sends detected | Low (if enforced) | High (account suspension) | Every post, comment, DM, and connection request requires human approval via the approval queue; no auto-send path exists in any workflow | MJ |
| 7 | Consent-record loss or corruption (Supabase outage/bug) | Low | High (undermines TCPA audit trail) | RLS + service-role-only writes to `consent_records`; Supabase log drain; treat consent rows as append-only (no deletes, only `revoked_at`) | MJ |
| 8 | Data breach — owner/contact PII exposure | Low | High | RLS enabled on all 10 tables; anon key restricted to INSERT-only on 2 tables; OAuth tokens encrypted in n8n credentials | MJ |
| 9 | Over-automation drift — a future workflow change accidentally removes the human-approval gate | Low | High | `approval_queue.decision` must be non-null before W4 or LinkedIn posting proceeds; code review requirement on any workflow touching send/publish nodes | MJ |
| 10 | FCC AI-voice disclosure NPRM (CG Docket 23-362) finalizes with stricter requirements | Medium | Medium | Build already includes AI-voice disclosure as best practice ahead of any final rule; monitor docket | MJ + counsel |
| 11 | Key-person bottleneck — MJ is the sole approver for all sends | High | Medium (delays, not compliance risk) | W6 daily digest with one-tap approve/edit/reject keeps approval friction low; consider a backup approver before scaling volume | MJ |
| 12 | Vendor downtime (Supabase, HubSpot, n8n, Retell) interrupts the pipeline | Medium | Low–Medium | n8n execution error alerts to Gmail; workflows are idempotent (safe to re-run); disabling W4 halts all outbound instantly if needed | MJ |
| 13 | TX Bus. & Com. Code §27.02(b) contract notice omitted from a signed contract | Low | High (statutory violation on the contract itself, separate from marketing compliance) | `tx_contract_notice` (`applies_to='contract'`) is a citation for a manual contract-template review before DocuSign deployment — it is deliberately NOT checked by the automated Compliance-Gate Agent, since `content_drafts` never holds actual contracts; counsel sign-off on contract templates is the real control here | MJ + counsel |
| 14 | Lexicon false positives (e.g., "guarantee" used in a benign, non-claim context) suppress legitimate content | Medium | Low | One instance tracked and accepted in `08-testing/compliance_redteam.md` item #41 ("we guarantee we'll show up on time" — a scheduling use of "guarantee"); refine further or add an allow-list exception if it proves disruptive in practice | MJ |
| 17 | **(Fixed)** Lexicon false negatives — the deductible-inducement regex (`tx_no_deductible`/`ok_no_deductible`) required near-adjacency to "deductible" and missed almost all realistic phrasing ("waive **your** deductible", "eat **the** deductible"); `us_no_guarantee`'s "100%" alternative had a word-boundary bug that missed "100% of homeowners..."; `us_no_legal_advice`'s bare "legal advice" alternative false-flagged the compliant disclaimer's own negated phrasing. Found by actually executing `08-testing/run_redteam.py` against the live table (0/44 clean on first run, 30/44 after) rather than trusting the regexes as written | Was: would have shipped silently — a contractor writing "We'll waive your deductible" in a real post would not have been caught | Would have been severe (undetected TX HB 2102 / OK 59 O.S. §1151.30 violations reaching publish) | All 5 bugs fixed and re-verified live: 44/44 fixtures now match expected status, 0 block-severity-marked-pass violations. Fixes folded into `01-supabase/006_seed.sql`. **Lesson: re-run `run_redteam.py` against any future rule change before trusting it** | MJ + counsel |
| 18 | **(Fixed)** `tx_contract_notice` scope mismatch — a contract-only disclaimer rule was firing against ordinary LinkedIn posts/newsletters/carousels because nothing distinguished "contract" content from "marketing" content in `compliance_rules` | Was: every TX-tagged post would be flagged for missing a notice that doesn't apply to it | Medium (operational friction, not a legal exposure, but would have trained the team to routinely dismiss `flag` results) | Added `compliance_rules.applies_to` (`content`\|`contract`); Compliance-Gate Agent now excludes `applies_to='contract'` rows when checking `content_drafts` | MJ |
| 15 | Storm-data false positives — a briefing or property report is generated from forecast/model data rather than confirmed radar/ground-truth hail, sending an inaccurate "your property was hit" message | Medium | Medium (credibility damage, wasted outreach) | Use MRMS radar hail-swath data (or a commercial product built on it), never forecast/model output, as the trigger source for `/webhooks/storm-alert` — see `03-n8n/fastapi_webhook_contract.md` | MJ |
| 16 | LinkedIn OAuth access-token expiry (~60 days outside the partner program) silently breaks publishing/analytics | Medium | Low–Medium (publishing pauses, not a compliance issue) | n8n's OAuth2 credential type auto-refreshes; monitor refresh failures via n8n execution-error alerts; the human-approval outreach path is unaffected by a token lapse | MJ |
| 19 | **(Fixed)** Live GitHub Pages lead-magnet forms were silently failing to capture consent on every submission — found only by curl-testing the deployed pages against the live Supabase REST API, not by loading them in a browser. Two independent bugs: (a) the `consent_records` batch insert's two array objects had mismatched keys (email object missing `ai_voice_disclosed`), which PostgREST rejects wholesale (`PGRST102`) — meaning **zero** consent rows had ever been written from any of the 5 live pages; (b) even with matching keys, `chk_consent_disclosure` would still reject the insert whenever the voice-consent checkbox was left unchecked (the default/common case), because `ai_voice_disclosed` was wired to the consent value itself instead of to whether the disclosure text was shown | Was: total, silent loss of the TCPA consent audit trail for every visitor who didn't opt into voice — i.e. almost everyone | Severe (no consent record exists for outreach that may still have been attempted downstream; undermines the entire consent-gated-dispatch design in W4) | Set `ai_voice_disclosed:false` on the email-channel object (for key parity) and `ai_voice_disclosed:true` unconditionally on the voice-channel object (it represents "disclosure was shown," distinct from `consent_given`); fixed in all 5 `05-lead-magnets/*.html` files, verified end-to-end via curl against the live project including the previously-broken declined-voice-consent case, and pushed to both the PR branch and `gh-pages` | MJ |
| 20 | **(Fixed)** `hail_report_request.html` posted directly to `/rest/v1/properties` from the browser using the anon key, which has no INSERT/SELECT policy on that table — the insert failed on every submission (confirmed via curl and RLS-scoped SQL testing) | Was: the hail-report flow's property-matching step silently no-opped on the live site | Low (feature degradation, not a compliance issue) — but the *fix considered and rejected* was granting anon INSERT/SELECT on `properties`, which would have opened an unauthenticated write surface into property records | Removed the client-side properties insert entirely; the captured address remains in `lead_magnet_submissions.answers` for a future service-role backend step to create/match the properties row and join `storm_events` | MJ |
| 21 | The live `restorationgc.net` privacy policy doesn't disclose what the (not-yet-filed) LinkedIn Community Management API integration does with data — Page analytics, OAuth token storage, comment/reaction data | Medium (LinkedIn's developer terms expect a connected API's data use to be disclosed) | Medium (could delay or block Standard-tier approval if flagged during LinkedIn's review) | Add a short paragraph to the privacy policy before filing the application — flagged in `06-linkedin/community_api_application.md`; needs a manual edit to the live WordPress site, no CMS tool is available from this session | MJ |
